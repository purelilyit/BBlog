<div class="container">
   <div class="row">
	   	<div class="col-md-7">
		     <p><%= image_tag @portfolio_item.main_image %></p>
		  </div>
	   	<div class="col-md-5">
			    <h3><strong><%= @portfolio_item.title %></strong></h3>
					<hr>
	        <p><%= @portfolio_item.body %></p>
	        <br>
	        <h5>Technologies used </h5>
	        <% @portfolio_item.technologies.each do |t| %>
	        		<p><%= t.name %></p>
	        <% end %>
					<%= link_to 'View All Portfolio Items', portfolios_path %>
		  </div>
   </div>
</div>
<%= params.inspect%><br> <hr>

<%= params[:controller]%><br> <hr>

<%= params[:action]%><br><hr>

<%= params[:id]%> <br><hr>

<%= session.inspect %>
